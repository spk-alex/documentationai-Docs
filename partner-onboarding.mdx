---
title: Partner Onboarding
description: How partners onboard themselves via the Partner Portal, plus the Admin API for scripted/automated onboarding.
---

SmartphoneKey delivers events to partner systems via webhooks. Most partners onboard themselves through the [Partner Portal](/partner-portal): sign in with Auth0, copy your API key, set a webhook URL, done. If you need scripted or automated onboarding — or any change beyond the URL (events filter, secret rotation, multiple webhooks) — an Admin API is available and documented further down this page.

## Quickstart: Self-Service via the Partner Portal

For partners who already have a SmartphoneKey account in Auth0:

1. **Visit the partner portal for your environment.** For example, dev is at `https://partner-portal.spk-dev.workers.dev/`. (Each environment has its own portal URL — your account manager will share the right one for stage / nonprod / prod.)
2. **Sign in with Auth0.** Click **Sign in with Auth0** and complete the OIDC flow.
3. **Copy the API key shown once.** On first sign-in, a tenant and an initial API key are created automatically. The full key is shown only once on this screen — copy and store it in your secret manager now. After this, only a masked prefix is visible.
4. **Set your webhook URL on the dashboard.** Enter the HTTPS endpoint in your system that will receive events. EventBridge will verify the URL.
5. **Done — events start arriving.** Once EventBridge confirms the subscription (status moves from `pending` to `verified`), events for your `orgId` are delivered to your URL.

You can return to the dashboard any time to rotate the API key or update the webhook URL.

See [Partner Portal](/partner-portal) for the full UI walkthrough.

## Receiving events

Events arrive at your webhook URL as HTTP POST requests carrying an AWS EventBridge envelope. The `detail` field includes a `partnerId` that matches your `orgId`, so you only receive events for your organization. Failed deliveries are retried with exponential backoff.

For payload shapes, signature verification, retry behavior, and security best practices, see [Setting Up Webhooks](/events-webhooks) and the [Event Catalog](/events-catalog).

## What partners can do without admin help

| Action | Self-Service (Portal) | Admin API |
|--------|-----------------------|-----------|
| Create tenant + initial API key | Yes (auto on first sign-in) | Yes (`POST /api-keys`) |
| View masked API key | Yes | Yes (list/get tenants) |
| Rotate API key | Yes | Yes |
| Set/update webhook URL | Yes | Yes |
| Update events filter | No | Yes (admin-only) |
| Rotate webhook secret | No | Yes (admin-only) |
| Multiple webhooks per partner | No | Yes (admin-only) |

## Programmatic onboarding (Admin API)

The rest of this page documents the Admin API. Use this for automation, integration tests, or admin operations the portal doesn't cover (events filters, secret rotation, multiple webhooks per partner).

### Prerequisites

- Admin API Key (`X-Admin-API-Key`) — stored in Doppler as `ADMIN_API_KEY`
- Admin JWT Token (`Authorization: Bearer <token>`) — must have `email` with `@smartphonekey.com` domain

Both headers are required on every admin-api request.

### Environments

| Environment | Admin API URL |
|-------------|---------------|
| Dev | `https://admin-api.spk-dev.workers.dev` |
| Nonprod | `https://admin-api.spk-nonprod.workers.dev` |
| Stage | `https://admin-api.spk-stage.workers.dev` |
| Prod | `https://admin-api.spk-prod.workers.dev` |

### Step 1: Create Partner (API Key)

Creating an API key automatically creates the partner (tenant).

```bash
curl -X POST "https://admin-api.spk-dev.workers.dev/api-keys" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "orgId": "acme-corp",
    "orgName": "ACME Corporation"
  }'
```

**Response:**
```json
{
  "apiKey": "xK7mP9qR2wT5vN8jL4hG6cF3bA0eD1iY...",
  "orgId": "acme-corp",
  "orgName": "ACME Corporation",
  "createdAt": "2026-04-02T10:00:00.000Z",
  "createdBy": "admin@smartphonekey.com"
}
```

Save the `apiKey` — it won't be shown again (only the prefix is visible in list endpoints).

**Notes:**
- One active API key per organization
- If the org already has an active key, you'll get `409 Conflict`
- The API key is 40 random alphanumeric characters

### Step 2: Set Up Webhook

Register a webhook URL to receive events for this partner. See [Setting Up Webhooks](/events-webhooks) for full configuration options including authentication and retry behavior.

```bash
curl -X POST "https://admin-api.spk-dev.workers.dev/tenants/acme-corp/webhooks" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://acme-corp.com/webhooks/smartphonekey",
    "events": [
      "Shadow Update"
    ]
  }'
```

**Response:**
```json
{
  "success": true,
  "aggregateId": "acme-corp",
  "version": 1
}
```

**Notes:**
- URL must be HTTPS in production (HTTP allowed in dev)
- A webhook secret is auto-generated if not provided
- The webhook is backed by AWS EventBridge — events matching the partner's `orgId` are delivered to the URL

#### Available Event Types

| Event Type | Source |
|------------|--------|
| `Shadow Update` | `spk.iot.shadow` |

`Shadow Update` is currently the only subscribable event type. See the [Event Catalog](/events-catalog) for a full description of the payload.

### Step 3: Verify Webhook

Check that the webhook was created and its status:

```bash
curl "https://admin-api.spk-dev.workers.dev/tenants/acme-corp/webhooks" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN"
```

**Response:**
```json
{
  "webhooks": [
    {
      "id": "32bc72f9-1676-4cd5-b568-15a39a80536e",
      "url": "https://acme-corp.com/webhooks/smartphonekey",
      "events": ["Shadow Update"],
      "status": "pending",
      "createdAt": "2026-04-02T10:01:00.000Z"
    }
  ],
  "total": 1
}
```

Status will transition from `pending` to `verified` once EventBridge confirms the subscription.

### Updating a Webhook

```bash
curl -X PUT "https://admin-api.spk-dev.workers.dev/tenants/acme-corp/webhooks/WEBHOOK_ID" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://acme-corp.com/webhooks/v2",
    "events": ["Shadow Update"]
  }'
```

At least one field (`url`, `events`, or `secret`) must be provided.

### Deleting a Webhook

```bash
curl -X DELETE "https://admin-api.spk-dev.workers.dev/tenants/acme-corp/webhooks/WEBHOOK_ID" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN"
```

### Listing Partners

```bash
curl "https://admin-api.spk-dev.workers.dev/tenants" \
  -H "X-Admin-API-Key: $ADMIN_API_KEY" \
  -H "Authorization: Bearer $ADMIN_JWT_TOKEN"
```

## How Events Flow to Webhooks

1. An action happens in the B2C API (e.g., lock opened, key assigned)
2. The event is published to AWS EventBridge with `source: "spk.api"` and `partnerId` matching the lock's organization
3. EventBridge matches the event against webhook subscription rules
4. The event is delivered to the partner's webhook URL

Events include `partnerId` in the detail so partners receive only events for their organization.

## Next Steps

- [Partner Portal](/partner-portal) — Self-service UI for partners to view their key and update their webhook URL
- [Events Overview](/events-overview) — Understand how SmartphoneKey publishes events via EventBridge
- [Setting Up Webhooks](/events-webhooks) — Full webhook configuration: authentication, delivery, retry behavior, and security best practices
- [Event Catalog](/events-catalog) — Complete reference for the highest-value event payloads
